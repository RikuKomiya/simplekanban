{
  "name": "@simplekanban/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/dev.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@libsql/client": "^0.15",
    "@simplekanban/db": "workspace:*",
    "@simplekanban/shared": "workspace:*",
    "better-auth": "^1.3",
    "drizzle-orm": "^0.44",
    "hono": "^4",
    "nanoid": "^5",
    "zod": "^3.25"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22",
    "typescript": "^5.8.3",
    "wrangler": "^4"
  }
}
