# Web dev server proxies /api to this server during `vite dev`.
# Configured directly in vite.config.ts (http://localhost:8787).
# No build-time env vars are required for the web app — the API client
# talks to the same origin via the shared createApiClient.
